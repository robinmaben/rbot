
class ResourceTracker(object):
    """
    Log meaningful crawler activity e.g. top-level pages
    """
    def log_crawl(self, resource):
        # Check if already crawled
        # If not, create new CrawledResource
        # If yes, update last_crawl_time
        pass

